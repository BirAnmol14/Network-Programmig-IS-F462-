To compile both file:
- make compile

To Start program:
- ./server in one terminal window (only one allowed)
- ./client in other terminal windows (multiple allowed)

Alternatively, make rserver or rclient can be used to run server and client respectively.

Note: Server needs to be active before clients run.
